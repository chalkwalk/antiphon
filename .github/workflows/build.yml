name: Build

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    # Linux is the only platform Antiphon has been developed and tested on.
    # macOS and Windows are in the matrix so the gap is visible rather than
    # unmeasured, and they are allowed to fail until the cross-platform work in
    # ROADMAP.md lands. A red cross on those two is the current honest state; do
    # not remove the matrix to make the badge green.
    continue-on-error: ${{ matrix.experimental }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux
            os: ubuntu-latest
            experimental: false
          # Both build and pass the full unit suite. They are still marked
          # experimental because nothing has yet *loaded* the plugin in a host
          # on either -- no window opened, no device opened, no jam joined --
          # and because the accessibility audit is excluded on them for want of
          # a window session. "Builds and tests clean" is a real result and the
          # name should say so; it is not the same as "supported".
          - name: macOS (build only, untested in a host)
            os: macos-latest
            experimental: true
          - name: Windows (build only, untested in a host)
            os: windows-latest
            experimental: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libasound2-dev libx11-dev libxcomposite-dev libxcursor-dev \
            libxext-dev libxinerama-dev libxrandr-dev libxrender-dev \
            libfreetype6-dev libfontconfig1-dev libcurl4-openssl-dev \
            libgl1-mesa-dev libglu1-mesa-dev ninja-build

      - name: Install Ninja (macOS)
        if: runner.os == 'macOS'
        run: brew install ninja

      # -g on top of Release, rather than RelWithDebInfo: the optimisation level
      # stays exactly what ships, so the timing of the audit hang is not
      # perturbed by the act of trying to observe it, but a backtrace from the
      # diagnostic step below has symbols. AGENTS.md is right that gdb on a
      # build without -g is address soup.
      - name: Configure (Linux and macOS)
        if: runner.os != 'Windows'
        run: >
          cmake -B build -G Ninja
          -DCMAKE_BUILD_TYPE=Release
          -DCMAKE_CXX_FLAGS=-g -DCMAKE_C_FLAGS=-g
          -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
          -DJUCE_BUILD_EXAMPLES=OFF -DJUCE_BUILD_EXTRAS=OFF
        shell: bash

      # Deliberately no -G here. With Ninja on a Windows runner, CMake picks up
      # whichever compiler is on PATH, which is MinGW g++ -- and JUCE refuses to
      # build under MinGW outright ("MinGW is not supported"). Letting CMake
      # choose its default generator gets Visual Studio and MSVC instead.
      - name: Configure (Windows)
        if: runner.os == 'Windows'
        run: >
          cmake -B build
          -DCMAKE_BUILD_TYPE=Release
          -DJUCE_BUILD_EXAMPLES=OFF -DJUCE_BUILD_EXTRAS=OFF

      - name: Build
        run: cmake --build build --config Release -j 4

      - name: Test
        if: runner.os == 'Linux'
        run: ctest --test-dir build -C Release --output-on-failure

      # The audit hangs here and only here: never on this machine, never under
      # any hostile environment tried locally, and the trace has now cleared
      # every candidate that static reading suggested. Reasoning has been wrong
      # twice; a stack is not an opinion. Runs the binary again, gives it 30s
      # (it takes about 2), and if it is still alive dumps every thread's
      # backtrace. Only fires when the tests have already failed, so it costs
      # nothing on a green run. Delete this step once the hang is fixed --
      # ROADMAP.md tracks it.
      - name: Dump stacks if the audit hung
        if: failure() && runner.os == 'Linux'
        run: |
          sudo apt-get install -y --no-install-recommends gdb
          audit=$(find build/test/AntiphonAudit_artefacts -name AntiphonAudit -type f | head -1)
          echo "running $audit under a watchdog"
          "$audit" > /tmp/audit.out 2>&1 &
          pid=$!
          for i in $(seq 1 30); do
            kill -0 "$pid" 2>/dev/null || break
            sleep 1
          done
          if kill -0 "$pid" 2>/dev/null; then
            # Two layers, because a diagnostic that can itself fail is no
            # diagnostic. The /proc read needs no privileges and always works;
            # it names the kernel wait-channel each thread is parked in, which
            # for a hang is most of the answer. gdb then gives the userspace
            # frames, if it can attach.
            echo "=== still running after 30s: per-thread wait channels ==="
            for t in /proc/"$pid"/task/*; do
              printf "tid %s state=%s wchan=%s\n" "$(basename "$t")" \
                "$(awk '/^State:/{print $2}' "$t"/status 2>/dev/null)" \
                "$(cat "$t"/wchan 2>/dev/null)"
            done
            echo "=== backtraces ==="
            sudo sysctl -w kernel.yama.ptrace_scope=0 >/dev/null 2>&1 || true
            sudo gdb -p "$pid" -batch -ex "thread apply all bt" 2>&1 | tail -120 \
              || gdb -p "$pid" -batch -ex "thread apply all bt" 2>&1 | tail -120 \
              || echo "(could not attach)"
            kill -9 "$pid" 2>/dev/null || true
          else
            echo "=== it exited on its own this time ==="
          fi
          echo "=== its output ==="
          tail -40 /tmp/audit.out || true

      # The accessibility audit drives a real JUCE component tree and needs a
      # window session to do it. Linux runners give it one well enough to run
      # headless; macOS and Windows runners do not, and it segfaults in the
      # window peer on both -- an environment limit, not a finding about the
      # code. Excluded there so these jobs report what they can genuinely
      # establish: that the audio, protocol and codec suites pass. Auditing on
      # those two needs a runner with a real session, which matters because they
      # are the platforms where the screen-reader work is reachable at all.
      # See ROADMAP.md.
      - name: Test (without the GUI audit)
        if: runner.os != 'Linux'
        run: ctest --test-dir build -C Release --output-on-failure -E accessibility-audit

      # There used to be a second, direct invocation of AntiphonAudit here, to
      # print the report. It ran the same binary ctest had just run, and it had
      # no timeout -- so when the audit hung, this step sat there for ten
      # minutes. ctest carries a 120s TIMEOUT on the test and prints the report
      # under --output-on-failure, which is the same information with a bound on
      # it. Do not reintroduce an unbounded second run.

      - name: Upload artefacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: Antiphon-${{ matrix.name }}
          if-no-files-found: warn
          path: |
            build/src/Antiphon_artefacts/**/VST3/*.vst3
            build/src/Antiphon_artefacts/**/CLAP/*.clap
            build/src/Antiphon_artefacts/**/Standalone/*

  lint:
    name: Format and tidy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libasound2-dev libx11-dev libxcomposite-dev libxcursor-dev \
            libxext-dev libxinerama-dev libxrandr-dev libxrender-dev \
            libfreetype6-dev libfontconfig1-dev libcurl4-openssl-dev \
            libgl1-mesa-dev libglu1-mesa-dev ninja-build clang-tidy

      # Pinned, and installed from PyPI rather than apt, because clang-format
      # is not stable across major versions: v18 and v20 disagree about `({})`
      # as a call argument, which is enough to fail a gate on code neither
      # author nor reviewer touched. apt gives whatever the runner image ships
      # (18 on ubuntu-latest today), so the version has to be stated. Keep this
      # in step with CONTRIBUTING.md, which tells contributors to install the
      # same one.
      - name: Install clang-format
        run: pip install "clang-format==20.1.8"

      # Blocking. The configuration is LLVM's defaults, which is what this
      # codebase was already written in, so a failure here means a file was
      # edited without running clang-format rather than a disagreement about
      # style -- provided the versions match, which is what the pin is for.
      - name: clang-format
        run: |
          clang-format --version
          clang-format --dry-run -Werror \
            src/*.cpp src/*.h test/*.cpp test/*.h tools/*.cpp

      - name: Configure (for the compile database)
        run: cmake -B build -G Ninja -DCMAKE_EXPORT_COMPILE_COMMANDS=ON

      # Advisory, deliberately. See ROADMAP.md, "Clear the clang-tidy backlog":
      # 142 findings at adoption. This becomes blocking -- with
      # WarningsAsErrors restored in .clang-tidy -- in the same commit that
      # takes the count to zero, and not before.
      - name: clang-tidy (advisory)
        continue-on-error: true
        run: |
          clang-tidy -p build --quiet src/*.cpp tools/*.cpp 2>&1 | tee tidy.log
          echo "findings: $(grep -cE 'warning:|error:' tidy.log || true)"
